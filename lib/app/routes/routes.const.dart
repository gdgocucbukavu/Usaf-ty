import 'package:usaficity/app/view/main.view.dart';

import '../view/view.dart';

class RoutePath {
  static dynamic auth = AuthScreen.route;
  static dynamic home = HomeScreen.route;
  static dynamic maps = MapsScreen.route;
  static dynamic schedule= ScheduleScreen.route;
  static dynamic profile= ProfileScreen.route;
  static dynamic overview = OverView.route;
  static dynamic mainview = MainView.route;
}
